#
遍历审查从前后端是否对应一致，前后端是否有没实现，数据库有无对应，生产环境，benchmark，代码质量，长期维护，可扩展，商业化，无虚拟数据，无硬编码,生产环境使用等所有角度无限次审查和   
遍历进行修复和构建，当修复和构建发现错误时候，你需要查看相应的整个模块的链路，以及对应连通的其他功能模块，修复完的时候必须清理端口，重新构建和部署服务器，保证不能有以下行为
    1. 缺乏增量验证 - 每次只修报告的问题，没有完整回归测试                                                                                                                                                                               
    2. 缺少自动化测试 - 没有单元测试/集成测试覆盖关键路径                                                                                                                                                          
    3. 类型系统薄弱 - 前端大量 any，后端参数无验证
    4. 代码审查不彻底 - 只看报告中的文件，没有端到端追踪
    5. 技术债务积累 - 快速迭代导致架构债务
    6. 缺乏预发布检查 - 没有强制性的 build check / type check而且必须执行
    1， - 每修复一个模块，同步审查相关链路
    2，- 修复后立即进行类型检查和语法验证
    3，- 确保修复不引入新的错误，你可以反复向我提问